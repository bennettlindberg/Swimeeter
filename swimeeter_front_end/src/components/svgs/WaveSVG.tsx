export function WaveSVG({ size }: { size: "SMALL" | "MEDIUM" | "LARGE" }) {
    switch (size) {
        case "SMALL":
            return (
                <>
                </>
            )

        case "MEDIUM":
            return (
                <>
                </>
            )

        case "LARGE":
            return (
                <>
                </>
            )
    }
}